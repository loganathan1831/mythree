from django.shortcuts import render
from django.http import HttpResponse
# Create your views here.
def loganathan(request):
    return HttpResponse('this is first')
def logan(request):
    return HttpResponse('this is first in first')